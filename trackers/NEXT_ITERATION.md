# Next iteration

Work on one of these packets, in this order of preference:

1. **Execute the widened native oracle packet** — run the eight differential commands listed in `trackers/NATIVE_WIDENED_ORACLE_PACKET_EVIDENCE.md`, archive the reports under `tests/differential/reports/smoke_native_input_widened/`, and upgrade the native supplement rows from `implemented_unverified` to `verified_smoke` only after the reports pass.
2. **Native metadata edge-policy packet** — declare and verify bounded policy for native metadata edge forms not yet admitted (if any), including explicit divergence logging for unsupported constructors or alternate map encodings.
3. **Non-JSON markdown-output admission beyond JSON input** — widen semantic markdown round-trip verification beyond JSON input only after source-format-specific heading-id and metadata policy is declared explicitly.

Do not open DOCX, PPTX, ODT, EPUB, Lua breadth, citeproc breadth, or broad writer-option work in the next round. The widened native oracle packet should be executed and archived before another broad frontier is opened.
