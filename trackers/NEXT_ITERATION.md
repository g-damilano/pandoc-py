# Next iteration

Work on one of these packets, in this order of preference:

1. **Widened native oracle admission packet** — now that the native route covers top-level `Pandoc` wrappers with metadata and the markdown/HTML writers can emit `Underline`, `SmallCaps`, `Quoted`, `LineBlock`, and `Null`, run oracle-backed differential evidence for native -> json/html/native and native -> markdown rails where comparator policy is explicit, then upgrade the native rows from `implemented_unverified` only after reports are archived.
2. **Native metadata edge-policy packet** — declare and verify bounded policy for native metadata edge forms not yet admitted (if any), including explicit divergence logging for unsupported constructors or alternate map encodings.
3. **Non-JSON markdown-output admission beyond JSON input** — widen semantic markdown round-trip verification beyond JSON input only after source-format-specific heading-id and metadata policy is declared explicitly.

Do not open DOCX, PPTX, ODT, EPUB, Lua breadth, citeproc breadth, or broad writer-option work in the next round. Native packet verification should be governed before another broad frontier is opened.
